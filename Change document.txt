You are acting as a senior enterprise product architect, UX strategist, content strategist, and implementation lead for an Agentic AI-based Regulatory Reporting platform for banks.

Your task is to implement the requested changes in one go across the Home page, Data Foundation, CAR Overview, Report Pack, Exceptions, Draft CAR Report, and Export PDF flow.

Primary objective
The most important review question from the client is:

“Does any of the tool actually work, or is it just static?”

Everything you implement must answer that question with a strong yes through:

working buttons

working state changes

working workflows

working refresh / regeneration

working download / upload

working validation

working submission and sign-off

working exception clearance

working report navigation

working PDF export

This must feel like a CFO-acceptable, impactful, trustworthy, enterprise-grade regulatory reporting product, not a prototype with dead controls.

Global design direction
Apply the same upgraded UI/UX principles already used on the improved home page:

denser, cleaner, more premium

less empty space

stronger visual hierarchy

better grouping and alignment

more CFO-friendly copy

audit-focused and operational

no vague or technical jargon where business language is better

all key actions clearly visible and meaningful

the product should feel like one connected workflow, not disconnected demo screens

1. Home page changes
Required changes
Rename “Regulatory Portfolio” to “Regulatory Report Portfolio”

Move this section to the center of the page so it becomes more prominent and easier to notice

Keep all current homepage information visible, but reorder and restructure using best-practice enterprise UX

Make the home page more CFO-friendly and impactful, while preserving density and clarity

Implementation expectation
Reorder sections without reducing information coverage

Improve prominence of the report portfolio

Keep the page balanced and executive-facing

Ensure the page feels like a command center, not a card dump

2. Data Foundation page changes
Core workflow change
The Data Foundation should now be a 4-step workflow, not 5.

Use this sequence:

Data Ingestion

Data Validation

Rule Engine

Report-Ready Data & Sign-off

Remove the standalone “Submission and Sign-off” step entirely and fold that functionality into step 4.

Use stronger CFO-grade wording if helpful, but keep the logic above.

2A. Download Template must work
The Download Template button currently does not work. Fix it.

It must:
download a general upload template

contain only:

column headers

simple guidance on how to fill it

be suitable for uploading values of data elements

be demo-ready and intuitive

Example columns:

Data Element ID

Data Element Name

Value


Also include a simple instruction row or sheet that explains how to fill it.

2B. Upload Template must work
The Upload Completed Template button must work.

It should:

accept the downloaded template back

parse uploaded rows

populate uploaded data elements into the ingestion flow

trigger validation on uploaded data

support re-upload after correction

This is critical for demo credibility.

2C. Validation must work for both ingestion methods
Validation must work on:

source system connected data

uploaded template data

This is critical and should be demonstrable.

Validation section changes
Exceptions Found should work

Replace Records to Attend with Records Checked

Records Requiring Attention should work

Add AI recommendation text for each exception

Add re-upload guidance or a small Re-upload Data button

Re-run Validation must work

Remove “categories”
Do not use the word categories.
Use Quality Checks instead.

Quality checks should only include (with small text to explain user what is being checked under each):
Completeness

Format & Structure

Duplicates


Remove:

Value integrity

Mapping

These do not make sense for the audience.

2D. Report-Ready Data Elements step must fully work
In step 4:

Submit for Sign-off must work

Approve must be renamed to Sign-off

Return for Correction must work with messaging to inform the maker that the checker has returned for correction the particular data element

the right-side status box should be moved above the table

the right-side summary must reflect correct live counts:

Ready

Submitted

Signed-off

Selection UX
Remove “Select all submittable”
Replace it with a small “Select all” button above the checkbox column, consistent with standard grid UX.

Workflow behavior
The entire workflow must function:

ready → submitted → signed-off

return for correction should move items back appropriately

counts and badges must update correctly

no dead buttons

This is one of the most important proof-of-functionality areas.

3. Report Pack changes
3A. Narrative tab
Add a button:

Re-create Narrative

This must work.

Also visibly indicate that this is generated by an AI Agent.
Examples:

“AI-generated narrative”

“Generated by Reporting AI Agent”

“Regenerate with AI Agent”

The user should understand that AI is actively doing something here.

3B. Exceptions tab
Current issue: the same exception appears 3 times.

Fix this.

Requirements
deduplicate exceptions

auto-refresh exceptions whenever user clicks the Exceptions tab

if user has already remediated an exception, it should disappear automatically

retain AI Recommendation

remove Approve and Apply

this section is only to show:

what exceptions exist

AI recommendation on how to resolve them

Resolution must be performed by the user via the actual workflow, not inside the exception card.

This should feel real and dynamic.

3C. Draft CAR Report linkages
In Draft CAR Report, under the Cover tab:

if there is a breach in report status,

clicking it should take the user to the Exceptions tab

the exception shown there must clearly explain the breach

This must be tightly linked and believable.

3D. Draft CAR Report sequence
The tab / section sequence must be:

Cover → Summary → S1 → S2 → S3 → S4 → S5 → S6

Fix any current incorrect ordering.

Also remove any placeholder text like:

“Certify the underlying data to populate this schedule.”

This is not demo-ready.

The whole report should work as one connected workflow, not as isolated placeholders.

3E. Draft CAR Report sign-off actions
In Draft CAR Report:

add Submit for Sign-off for Maker

add Sign-off for Checker

both must work

Remove:

sign-off comment

Keep the workflow clean and credible.

3F. S6 blank computation
If no computation exists in S6, do not leave it blank.

Show suitable English text, for example:

“No additional computation applies for this line item.”

“This line is directly sourced and does not require a separate computation.”

Do this wherever blank computation cells exist.

3G. Export PDF must work and be trustworthy
The PDF export must work seamlessly.

Output requirement
PDF sequence must be:

Cover + Summary together as the first page using Uniqus branding/template already used

Then Schedule 1 (with full name of each schedule)

Schedule 2

Schedule 3

Schedule 4

Schedule 5

Schedule 6

Expectations
values must match the tool exactly

formatting must be concise, readable, and CFO-trustworthy

remove Preview Draft Report

only keep a clean working export/download action

exported artifact should feel polished and client-ready

4. CAR Overview page changes
Required changes
Rename “Sections Signed” to “Sections Signed-off”

In Key Observations, the control:

“Last computed 8 min ago · refresh”
currently does nothing

This must work.

Expected behavior
On click:

refresh the underlying computed state

update the timestamp

if observations depend on current data, regenerate or recompute them

visibly show that the system is active, not static

This is highly important because a dead refresh button damages trust immediately.

5. Functional proof requirements
Implement actual working interactions for all of the following:

template download

template upload

validation run

re-run validation

exceptions refresh

re-upload path

submit for sign-off

sign-off

return for correction

narrative regenerate

CAR overview refresh

breach → exceptions navigation

PDF export

If backend is mocked or local-state-driven, that is acceptable for now, but the UX must behave as a real working product.
Do not leave any dead buttons or fake controls.

6. UX and content guidance
Use business-friendly, CFO-facing language
Avoid vague or technical wording.

Prefer:

Report-Ready Data Elements

Regulatory Report Portfolio

Signed-off

Quality Checks

Records Checked

Records Requiring Attention

AI Recommendation

Re-create Narrative

Go to Exceptions

Export PDF

Avoid
static demo language

placeholder instructions

duplicate exceptions

dead refresh actions

hidden critical status

disconnected flows

inconsistent terminology like approve vs sign-off

Everywhere that AI is being used -- it should be highlighted to the user -- for example, data validation, exception etc -- each and every AI component should be called out appropriately
Make the product feel demonstrably functional and cohesive in one pass.

If code structure needs refactoring to make this work properly, do the refactor.